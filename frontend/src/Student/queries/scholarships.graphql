query Scholarship($periodId: ID!) {
  scholarshipByPeriod(periodId: $periodId) {
    id,
    name,
    scholarships{
      id,
      name,
      gpa,
      yearStatus,
      requiredPhotos,
      description,
      fulltime,
      renewable,
      requiresEssay,
      degreePrograms
    }
  }
}
